# Deferred Work

Items flagged during reviews that are real but not actionable in the story where they were found. Each entry notes the origin, the target story for pickup, and the one-liner reason.

---

## Deferred from: code review of story 1-0-define-yaml-config-schemas-spike (2026-04-14)

- **Duplicate YAML keys at same level.** PyYAML `safe_load` accepts duplicates silently (last-wins), causing silent data loss. **Target: Story 1.6 (config loader).** Loader can use `yaml.SafeLoader` subclass that rejects duplicates.
- **Unicode case-fold edge case (Turkish locale, dotted/dotless i).** `.casefold()` handles most Latin scripts correctly; Turkish is the documented failure mode. **Target: Story 1.6 (config loader) / Story 4.2 (exclusion matcher).** Address if a user reports the bug.
- **Modes directory exists as a file (AV quarantine / user error).** Undefined behavior when `%LOCALAPPDATA%/nova/modes` is not a directory. **Target: Story 1.6 (config loader).** Loader should detect and produce a user-facing error distinct from "no modes found".
- **CRLF trailing whitespace in string values.** Not applicable in current T1 schema (no multiline strings), but re-check if future fields add them. **Target: whichever story adds multiline string fields.**
- **First-run copy file-lock / antivirus interference.** Edge case during initial setup where target path is locked. **Target: Story 2.1 (setup.bat / first-run).** Setup should retry with clear error if locked.
- **Reserved Windows filenames (CON, NUL, AUX, etc.) in mode names.** Slugified mode names could collide with reserved names. **Target: Story 2.3 (guided mode wizard)** — validate at creation time; **Story 1.6 (loader)** — surface clear error if encountered on existing file.

---

## Deferred from: code review of story 1-1-project-scaffolding-and-package-setup (2026-04-14)

- **Hatchling default sdist includes `_bmad-output/`, `design-artifacts/`, and the full planning tree.** Only matters if N.O.V.A. is ever published to PyPI — project-context rules this out for T1. **Target: whichever story turns on package publishing (none currently planned).**
- **PEP 735 `[dependency-groups]` migration.** uv and PDM are converging on `[dependency-groups]` over `[project.optional-dependencies]` for dev deps. **Target: monitor — revisit when uv's guidance stabilizes or when Story 1.11 touches dep config.**

---

## Deferred from: code review of story 1-2-domain-exceptions-and-shared-types (2026-04-14)

- **`NovaError.cause` is not preserved across `pickle` / `copy.deepcopy` round-trips.** `BaseException.__reduce__` only serializes `self.args`; the constructor's `cause=` kwarg is dropped on round-trip. **Target: whichever story introduces multiprocessing, subprocess workers, or remote-process IPC (none in T1 — architecture is single-process).** Document the limitation in `core/exceptions.py` module docstring; revisit only if cross-process exception transport ever becomes a requirement.
- **AST isolation test crashes on namespace / zipped / frozen module deployments.** `inspect.getsourcefile()` returns `None` (or a zip-internal path that `Path.read_text()` cannot open) under `pyinstaller --onefile`, `zipapp`, or namespace-package layouts. Today the test asserts non-None. **Target: whichever story introduces packaging beyond `uv sync` + `uv run nova` (none in T1).** When a packaging story lands, switch to `importlib.resources.files(module).joinpath(...).read_text()` or guard with `pytest.skip` when source is unavailable.

---

## Deferred from: code review of story 1-3-event-bus-and-typed-event-definitions (2026-04-14)

- **Frozen dataclass `__hash__` and `__eq__` contracts not locked by tests.** `@dataclass(frozen=True)` auto-generates `__hash__`; a future field of unhashable type (e.g. `dict`, `list`, a mutable dataclass) breaks `hash()` at runtime. **Target: whichever downstream story first uses events as dict keys or set members (Story 3.5 Nerve routing is the likely first consumer).** Add parametrized hash/equality tests then.
- **Pickle / `copy.deepcopy` round-trip of events not tested.** Not used in T1 (single-process, no IPC, no durable event persistence). **Target: Story 1.8 AuditLogger if it ever serializes event references.** Architecture currently records actions, not events, so this may never become relevant.
- **Field-schema string comparison is fragile under formatter changes.** `from __future__ import annotations` stores `f.type` as raw source text; reformatting or switching between `Optional[str]` and `str | None` breaks the test without a real type-contract change. **Target: revisit if the test starts churning during refactors.** Migration path: switch to `typing.get_type_hints(cls)` for runtime type resolution.

---

## Deferred from: code review of story 1-4-sqlite-storage-engine (2026-04-14)

- **Corrupt-DB-that-opens-but-fails-on-pragma path not covered by tests.** `sqlite3.connect` is lazy — it can succeed on a corrupt file and the first pragma raises `sqlite3.DatabaseError`. The inner cleanup in `_open_and_configure_sync` is correct by inspection (closes conn before re-raise) but has no regression test. The WAL-silent-fallback test (`test_wal_verification_rejects_silent_fallback`) exercises an adjacent path but not the corrupt-DB one. **Target: Story 5.5 (SQLite corruption recovery flow) — naturally needs a corrupt-DB fixture.** Reuse the fixture to backfill the pragma-failure regression test here.
- **DDL + implicit-transaction failure-carryover edge cases not tested.** `isolation_level="DEFERRED"` auto-BEGINs on first DDL/DML; a failed `execute()` leaves the implicit transaction in a rolled-back state. Subsequent calls work (sqlite3 carries on), but there's no test. Happy path is locked. **Target: Story 3.1+ (Brain adapter) — first real multi-statement consumer.**
- **Windows long-path (`MAX_PATH` >260 char) and directory-as-db-path not tested.** Both produce distinct errors (OSError from mkdir vs. `sqlite3.OperationalError("unable to open database file")`), both translate to `StorageError("start failed")`, but neither is locked by a test. **Target: whichever story introduces packaging / user data dir hardening** (Story 2.1 setup.bat, Story 5.5 corruption recovery). Low priority — current error-translation is generic enough to handle both.

## Deferred from: code review of story 1-5-migration-runner-and-initial-schema (2026-04-14)

- **`_FILENAME_RE` only matches 3-digit prefixes — silent skip for v1000+ migrations.** Pattern `r"(?P<num>\d{3})_[a-z][a-z0-9_]*\.py"` requires exactly 3 digits. A future `1000_something.py` is silently ignored (regex returns None → file is treated as "not a migration") with no error. **Target: post-T1 (we will not author 1000+ migrations in T1).** Fix when refactoring the runner for future scale: change to `\d+` and let the file/version cross-check fire.
- **`test_transaction_rolls_back_on_cancellation` is timing-dependent (`asyncio.sleep(0.05)` race).** Test creates a task, sleeps 50ms, then cancels — expecting cancellation to land inside the inner `asyncio.sleep(10.0)` rather than during the `INSERT`. Under CI load, the cancel could fire during `INSERT` and the assertion path may differ. **Target: test-hygiene pass.** Replace the wall-clock sleep with an `asyncio.Event` set after the INSERT completes inside the task.
- **`_write_pkg` helper leaves synthetic packages in `sys.modules` after teardown.** `monkeypatch.syspath_prepend` reverts `sys.path` but `importlib.import_module` permanently caches the module. Per-test counter prevents collision in current usage. **Target: test-hygiene pass.** Add `monkeypatch.delitem(sys.modules, ...)` cleanup inside the helper so the cache is torn down with the test.

## Deferred from: code review of story 1-6-config-loader-and-immutable-novaconfig (2026-04-14)

- **`!!python/object` YAML tag rejection not test-locked.** `SafeLoader` rejects it by construction; adding an explicit regression test is belt-and-suspenders. **Target: whichever story next widens the loader surface, OR a test-hygiene pass.** Low risk — the constructor-map for unsafe tags is inherited from `SafeLoader`.
- **Broken-symlink mode files skipped silently.** `entry.is_file()` returns `False` on a broken symlink → `_load_modes` silently skips it with no warning. Debugging trap on first-run misconfigurations. **Target: Story 2.1 (setup.bat / first-run)** — that's where symlink-aware setup would first matter. Fix: add `entry.is_symlink() and not entry.exists()` branch with a warning.
- **URL validation allows embedded control chars / NUL bytes.** `"http://\x00malicious"` passes the scheme-prefix check. Downstream browser behavior is undefined. **Target: Story 6.5 (Full mode configuration: folders & URLs)** — Story 3.6 (Mode Restore & App Launching) shipped launch-only restore and explicitly does NOT open URLs (the `mode_config.urls` field is read and ignored with an INFO log). The use site for URL opening lands with Story 6.5; control-char screening belongs there. Fix: `any(ord(c) < 0x20 or ord(c) == 0x7F for c in entry)` rejection in whichever validator the restore path consults.

## Deferred from: code review of story 1-7-capability-tier-state-machine (2026-04-15)

- **Canonical reason string `"2 consecutive API failures"` hardcodes the default threshold.** If an operator sets `degrade_failure_threshold=5`, the emitted `TierChanged.reason` still says "2 consecutive", misleading Story 5.4's tier-notice renderer. The closed canonical set in AC #7 is load-bearing; threshold-aware interpolation would break the closed-set guarantee. Not exercised in T1 (no consumer sets a custom threshold). **Target:** whichever story first ships an operator-facing threshold knob. Fix options: (1) validate `degrade_failure_threshold == 2` at construction and require a story-level schema change to widen; (2) expand the canonical set to include threshold-parameterized variants as a deliberate schema update.
- **`CancelledError` mid-`event_bus.emit` leaves tier advanced with no observer event.** Tier state is mutated before the `emit` begins; if the coroutine is cancelled inside `emit`, downstream subscribers never learn of the transition. Edge case during process shutdown where the composition root is tearing down subscribers anyway. **Target:** Story 3.10 (Crash Recovery) or Story 8.3 (Tier Recovery & Catch-up Briefing). Fix options: `asyncio.shield` around the emit, or a catch-up replay on next boot that reconciles last-known-tier vs. current-state.
- **`test_emitted_event_has_source_nerve` tests a `TierChanged` field default rather than a behavior `TierManager` owns.** The test passes because `source="nerve"` is a `field(default=..., init=False)` in the event class, not because `TierManager` sets it. A future event-schema change would fail this test pointing at `TierManager` instead of the real cause. Test-quality polish, not a functional regression. **Target:** next test-hygiene pass.

## Deferred from: code review of story 1-8-audit-logger (2026-04-15)

- **AST regex on SQL constant misses string-concat / f-string violations.** `test_audit_module_uses_only_insert_sql_no_update_or_delete` walks `ast.Constant(str)` nodes; `f"DELETE FROM {TABLE}"` where `TABLE = "audit_log"` lives as a separate constant would slip through. Fundamental limit of static-analysis on string literals; current guard catches the realistic regression class. **Target:** next test-hygiene pass — document the limitation in the test docstring.
- **`dir()`-based `test_audit_logger_only_public_method_is_log_action` is brittle and redundant with the AST guard.** The companion `test_audit_logger_class_has_no_mutating_methods_beyond_log_action` (AST-walk of the class body) is the real lock. The `dir()` version would break on harmless future additions (public properties, class-level type aliases). **Target:** next test-hygiene pass — drop the `dir()` test once review confirms the AST guard is sufficient.
- **`_FailingExecuteEngine.execute` accepts `params: Any` instead of mirroring the real engine's `_reject_scalar_string_params` guard.** If `audit.py` regressed to passing a bare string in `params`, the real engine would raise `StorageError` (which audit swallows!) and the failure-mode tests would still pass because the fake raises its pre-canned exception. **Target:** next test-hygiene pass — add `_reject_scalar_string_params(params)` in the armed branch of the fake.
- **`test_log_action_signature_pins_action_type_to_enum` doesn't catch loosen-to-`ActionType | str`.** Catches loosen-to-`str`. Subsumed if the E6 patch lands (runtime `isinstance(action_type, ActionType)` guard) — once that's in, runtime widening is impossible regardless of declared annotation. **Target:** dies automatically when E6 patch lands; no separate work needed.
- **`json.dumps` non-string-key error message is misleading.** Caller passing `details={1: "x"}` gets `TypeError` whose message mentions serialization, not the `Mapping[str, object]` schema violation. Diagnostic quality only; same propagation surface as the documented `TypeError` path. **Target:** docstring tweak in a future polish pass — extend the `Raises:` block to mention non-string keys explicitly.
- **Future LogRecord reserved-name collision risk in `extra={...}`.** Today's keys (`action_type`, `target`, `result`) are safe; a future maintainer adding `extra={"message": ...}` or `extra={"name": ...}` would hit `KeyError` from the logging module — in the WARNING path which is precisely where you least want a secondary exception. **Target:** add a code comment when a contributor next edits this `extra` dict.
- **Concurrency test gap: cancellation mid-`execute` while engine `_tx_lock` is held.** `test_log_action_concurrent_writes_all_land` only verifies `gather` of three writes; doesn't exercise outer-transaction-holds-lock + audit-task-cancelled-mid-acquisition. Behavior is correct today (engine releases the lock on `CancelledError`); test gap only. **Target:** next test-hygiene pass — add a regression lock for this realistic shape.
- **"Engine not started" failure mode test is merged into the generic StorageError-swallow test.** AC #15 lists 4 distinct observational-failure tests; the implementation has 4, but bullets #1 ("engine not started") and #2 ("execute monkeypatched to raise StorageError") are merged. Semantic coverage is identical — `StorageError` swallow path is locked. **Target:** next test-hygiene pass if the duplication ever feels useful; otherwise leave as-is.
- **Test-name drift from spec's "Locked by `<name>`" anchors.** Three tests exist under different names than the spec referenced; semantics match exactly. Affects only grep-ability of the spec's "Locked by" anchors. **Target:** test-hygiene pass — rename to match the spec strings, OR retroactively edit the spec to match the implementation names.

---

## Deferred from: code review of story 1-11-ci-quality-gate-automation (2026-04-15)

- **Concurrency cancels mid-flight validation on `main` branch too.** Rapid merges could cancel a predecessor's validation before it completes. Spec AC #1 locked the pattern; solo-dev cadence makes the risk negligible today. **Target:** revisit if multi-dev workflow begins. Fix: `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` or scope `group:` by `github.head_ref || github.run_id`.
- **`push: branches: ['**']` + `pull_request: {}` double-fires CI on every PR commit.** 2x runner minutes per PR. Spec-locked trade-off. **Target:** restrict `push:` to `main` only if minute budget tightens.
- **`windows-latest` silent runner-image upgrade risk.** `pwsh` default-shell semantics could drift on a `windows-2022` → `windows-2025` roll. Workflow uses no shell-specific syntax today. **Target:** pin `runs-on: windows-2022` or add `defaults: { run: { shell: bash } }` if a runner upgrade breaks a step.
- **`--cov=nova` vs `source = ["src/nova"]` two-strategy coverage resolution.** Agrees today; could drift if packaging layout changes. **Target:** consolidate to `--cov=src/nova` during next touch to coverage config or pytest invocation.
- **Coverage `parallel = false` collides with pytest-xdist if added later.** Spec-locked for T1. **Target:** whichever story introduces pytest-xdist — flip to `parallel = true` and add `coverage combine`.
- **`exclude_lines = ["\\.\\.\\."]` unanchored regex.** Matches any triple-dot in a source line, not just bare Ellipsis bodies. Convention-matching with coverage.py docs. **Target:** tighten to `"^\\s*\\.\\.\\.\\s*$"` if over-exclusion appears in reports.
- **`.gitignore` CRLF rule missing from `.gitattributes`.** Current test-suite behavior is CRLF-safe (verified: `Path.read_text()` universal newlines + `splitlines()`). **Target:** add `.gitignore text eol=lf` to `.gitattributes` next time it's touched.
- **`_extract_canonical_commands` prefix-pairing could silently mis-pair if project-context.md chain reorders.** `assert not missing` catches total-count regressions but not mis-pairing. **Target:** tighten to assert exactly-one-segment-per-prefix [tests/unit/test_ci_workflow.py:99-108].
- **`test_workflow_runs_canonical_commands_in_order` lacks distinctness assertion.** `positions == sorted(positions)` passes trivially if prefixes collide and multiple labels match the same step. Low-risk (all prefixes unique today). **Target:** add `len(set(positions)) == len(positions)` in next test-hygiene pass.
- **Canonical-commands regex breaks if project-context.md reformats the full-verify line to a fenced code block.** **Target:** generalize parser or pin section-heading anchor when project-context.md is next restructured.
- **`uv >= 0.5.11` minimum documented but not enforced.** Dev on older uv hits cryptic "unsupported lockfile revision" error. Spec AC #10 was docs-only. **Target:** Story 2.1 (setup.bat) — include a `uv --version` parse + minimum check during first-run setup.
- **AC #13 extra parametrized test added beyond spec literal list.** `test_workflow_step_starts_with_canonical_command` + `test_workflow_runs_canonical_commands_in_order` (vs. spec's single test). Superset; accounts for +9 of the "+27 tests vs. AC #15's +18 prediction" delta. Noted as spec-over-delivery; no action required.

---

## Deferred from: code review of story 1-10-composition-root-and-cli-entrypoint (2026-04-15)

- **Reserved Windows filenames in `--data-dir`.** `Path("CON").resolve()` on Windows silently returns a device-path; later `mkdir(parents=False)` on `logs/` under a reserved device raises a cryptic `OSError` rather than a clean `ConfigError`. **Target:** Story 2.1 (setup.bat + first-run wizard) owns input validation for the data-dir path.
- **`NovaApp.close` closure has loop-binding hazard.** The closure captures `storage` and awaits `storage.close()`, which is bound to the asyncio loop that constructed it. Cross-thread / cross-loop teardown attempts (signal handlers, REPL debugging) would fail with cryptic errors. Documented via the "single loop lifetime" note in `app.py` docstring; no enforcement today. **Target:** whichever story introduces multi-loop or signal-handler teardown (none planned).
- **`_remove_handlers_by_name` does not guard `handler.close()` failures.** If `handler.close()` raises (e.g., externally deleted log file), the Phase A → Phase B handoff stops mid-loop, leaving the root logger with a mix of dead and live handlers. Low probability in practice. **Target:** next test-hygiene pass.
- **`NovaApp(frozen=True, slots=True)` is not picklable.** The `close` field is a closure; closures are never picklable. Hazard only materializes if a future story tries `multiprocessing` / `ProcessPoolExecutor` with `NovaApp`. T1 architecture is single-process. **Target:** whichever story adds cross-process IPC (none planned in T1).
- **Stray `_tmp_story-1-6.patch` at repo root.** Pre-existing leftover from Story 1.6; noticed during Story 1.10 review. Not produced by this story. **Target:** next housekeeping commit — just delete it.

---

## Deferred from: second-pass code review of story 2-1-setup-script-setup-bat (2026-04-15)

- **No integration test for uv-install failure branch.** The `:fail_uv_install` path now logs installer output to `%TEMP%\nova-uv-install-<N>.log` and cites it in the friendly message, but there's no regression test exercising that branch — mocking `powershell irm | iex` from batch requires a shim (fake `uv` + PATH manipulation + a failing `where uv` assertion) that's disproportionately complex for a branch that fires only on network / execution-policy failure. **Target:** next test-hygiene pass, OR a story that introduces a broader `setup.bat` fixture harness (shared setup-flow helpers) — whichever lands first.
- **No integration test for py-launcher-only 3.13+ Python installations.** The `:check_py_version` subroutine now accepts any 3.12+ reported by `py -3`, but mocking a host where `py -3` exists and reports 3.13 while `python` is absent needs a PATH-override shim or a synthetic `py.exe` on PATH. **Target:** same test-hygiene pass as above; the shim, once built, covers both this branch and the uv-install-failure branch. Until then, the `:check_py_version` logic is unit-reasoned-about but not test-locked.

---

## Deferred from: code review of story 2-1-setup-script-setup-bat (2026-04-15)

- **`%RANDOM%` collision on concurrent setup invocations.** `ROLLBACK_LIST` uses `%TEMP%\nova-setup-rollback-%RANDOM%.txt`; `%RANDOM%` is a 15-bit PRNG seeded from the clock — two cmd.exe processes started in the same tick can produce the same value, leading to cross-run rollback-list contamination. **Target:** whichever story first introduces multi-user or concurrent-setup scenarios (none planned in T1 — solo-user product). Fix: combine `%RANDOM%%RANDOM%%TIME:~-2%` for 30+ bits of entropy.
- **`sort /r` does not guarantee reverse-depth order for nested DIR entries in rollback.** Works today only because Story 2.1 tracks a flat set of siblings (`modes`, `backups`, `logs`). If Story 2.3 or later adds nested tracked directories, a shallow DIR may be attempted before its deeper child, and `rmdir` (no `/s`) silently skips non-empty dirs — leaving partial state. **Target:** whichever story first adds nested tracked directories in setup.bat. Fix: two-pass rollback — iterate all FILE entries (any order), then DIR entries in reverse insertion order.
- **`%~dp0` trailing-backslash strip breaks when script is at a drive root.** `setup.bat` at `C:\setup.bat` yields `REPO_ROOT=C:` (drive-relative), making `pushd "%REPO_ROOT%"` a drive-relative CWD change rather than an absolute one. **Target:** unlikely to trigger (nobody installs products at drive root); fix when next touching the script. Fix: only strip when the result is longer than 3 characters.
- **Empty `config\modes\*.yaml` glob expands to literal pattern on some cmd versions.** `for %%F in ("%REPO_ROOT%\config\modes\*.yaml")` with no matches can pass the literal `*.yaml` to `:copy_if_missing`. Repo currently ships `coding.yaml`, so never triggers. **Target:** housekeeping pass. Fix: guard with `if exist "%REPO_ROOT%\config\modes\*.yaml"` before the `for`.
- **UNC paths (`\\server\share\...`) silently pass validation despite "not contracted" docstring.** The anchor-shape check fails (length > 3), falls through to `_validate_segment(anchor)`, but `\\server\share\` contains no invalid chars, doesn't end in dot/space, and isn't a reserved name — so it passes. The scope fence accepts this; current behavior is "accept without contract." **Target:** whichever story first supports roaming profiles / network shares (none planned in T1). Fix: either explicitly accept with tests, or explicitly reject with a clear reason.
- **Leading space / leading dot in path segments not rejected.** Windows accepts both at the API level but `mkdir " foo"` trims leading spaces via `CreateDirectoryW` in some versions, silently producing a directory at the wrong path. AC #17 only scopes trailing dots / spaces. **Target:** next path-hygiene pass. Fix: extend `_validate_segment` with leading-space rejection.
- **Unicode full-width reserved names (`ＣＯＮ`, `ＰＲＮ`, etc.) not caught by `.lower()`.** `.lower()` does not NFKC-fold full-width Latin. User would need to deliberately type full-width — low impact. **Target:** next path-hygiene pass. Fix: apply `unicodedata.normalize("NFKC", segment).lower()` before checking against `_RESERVED_WIN_NAMES`.
- **`_force_utf8_stdout` catches only `OSError`/`ValueError`.** On embedded Python builds without the utf-8 codec, `reconfigure(encoding="utf-8")` raises `LookupError`, which is not caught. Also, pytest-wrapped streams that expose `reconfigure` with a different signature could raise `TypeError`. **Target:** next test-hygiene pass. Fix: broaden suppression to include `LookupError`, `TypeError`.
- **`test_state_a_output_has_no_exclamation_marks` is a byte-level check that could false-fail on future copy containing `!`.** Tests implementation detail of copy rather than observable behavior. **Target:** next test-hygiene pass or when the wizard copy is materially expanded (Stories 2.2–2.4). Fix: scope to the Text object's `.plain` content rather than the rendered stream, or drop in favor of review-time style checking.
- **`mkdir ... 2>nul` swallows root-cause of filesystem errors.** Rollback path fires correctly but the user-facing message is "Couldn't create data subdirectories" with no underlying reason captured — hard to triage antivirus / roaming-profile interference. **Target:** diagnostic-improvement pass. Fix: capture stderr to a temp file and echo it when rollback fires.

---

## Deferred from: implementation of story 2-1-setup-script-setup-bat (2026-04-15)

- **`.gitattributes` missing repo-wide line-ending normalization, especially `*.bat text eol=crlf`.** First `setup.bat` revision was written with LF-only endings; cmd.exe silently mis-parsed each line as a separate (failing) command with cryptic `'st-run' is not recognized` errors rather than failing the script outright. Every future `.bat` touch risks reintroducing the bug. **Target:** next housekeeping commit, OR whichever story next touches repo-wide hygiene. Fix: add `.gitattributes` with at minimum `*.bat text eol=crlf` (and consider `*.cmd text eol=crlf`, `*.ps1 text eol=crlf` while there). Story 1.11 already flagged a related `.gitignore text eol=lf` entry for the same file — bundle them.
- **No regression test that `setup.bat` has CRLF line endings.** A line-ending regression would manifest as the cmd.exe mis-parse failure, which `test_setup_bat_is_idempotent` would catch only if it runs on CI (it's marked `@pytest.mark.windows_only`). On a dev loop where integration tests are skipped, a maintainer editing `setup.bat` through an editor that auto-converts to LF would see no local failure. **Target:** same housekeeping story as `.gitattributes`. Fix options: (a) an AST-style byte-level test that asserts `setup.bat` has CRLF + `.gitattributes` enforces it, or (b) trust the `.gitattributes` + pre-commit hook rather than adding a test.
- **`NOVA_SETUP_SKIP_SYNC=1` is a production script carrying a test-only hook.** The flag exists because the idempotency integration test runs `setup.bat` while pytest holds `.venv/Scripts/pytest.exe` open, and `uv sync` would then race. Document it more prominently (or move the hook out of the production script via a wrapper invocation) if the flag ever leaks into user-facing docs. **Target:** test-hygiene pass after Story 2.5 lands (at which point the full wizard flow is in place and the integration test shape may change enough to revisit the hook's placement).

---

## Deferred from: code review of story 1-9-port-interfaces-and-shield-stub (2026-04-15)

- ~~**`Mapping[str, object] | None` on `ActionRequest.details` permits runtime `dict` mutation.**~~ **Closed by Story 3.6 (2026-05-05).** `ActionRequest.__post_init__` now wraps caller-supplied `details` in `types.MappingProxyType` after a defensive `dict(...)` copy (via `object.__setattr__` per the dataclasses-on-frozen escape hatch). Caller-side mutation of the original dict cannot reach back into the request. See [3-6-mode-restore-and-app-launching.md](3-6-mode-restore-and-app-launching.md).
- **`DeletionPreview` has no nonce/id binding to the originating `delete_matching` call.** Two concurrent preview flows produce indistinguishable previews; `confirm_deletion(preview_A)` could execute preview_B semantics. **Target: Story 5.2 (Selective Forget).** Add `preview_id: str` (uuid4) field; adapter verifies id on confirm.
- ~~**`NervePort.route_command` returns `None` — error surface undocumented.**~~ **Closed by Story 3.5 (2026-05-05).** `route_command` now returns `CommandOutcome` — a closed two-member `StrEnum` (`CONTINUE` / `EXIT`) declared at [`src/nova/systems/nerve/models.py`](../../src/nova/systems/nerve/models.py). Every handler declares its outcome explicitly; the REPL inspects the return with `if outcome is CommandOutcome.EXIT`. The "error surface" question dissolves — handlers either return CONTINUE/EXIT or raise domain exceptions per the existing port-error contract. See [3-5-nerve-command-routing-and-session-lifecycle.md](3-5-nerve-command-routing-and-session-lifecycle.md).
- **`VoicePort.generate_prose_enrichment: str | None` cross-module test missing.** No guard ensures `BriefingViewModel.prose_enrichment: str | None` aligns with the port return type if one is later narrowed. **Target: Story 3.3 (BriefingViewModel rendering).** Add a contract test asserting the two annotations match.
- **Stringly-typed timestamps across all domain models.** `Session.ended_at: str`, `MemoryItem.created_at: str`, `WorkspaceSnapshot.captured_at: str`, etc. Throws away type-system benefits at every boundary; callers must reparse to sort/diff. **Target: project-wide refactor; revisit when a caller's ergonomics demand `datetime.datetime`.** The string form is load-bearing for SQLite roundtrip — a wrapper type with implicit ISO conversion is the likely fix.
- ~~**`Command.verb: str` is a free-form string instead of `Literal[...]` or enum.**~~ **Closed by Story 3.4 (2026-05-05).** `CommandVerb(StrEnum)` ships as a 16-member closed vocabulary in [`src/nova/systems/skin/models.py`](../../src/nova/systems/skin/models.py); `Command.__post_init__` enforces the closed vocabulary at runtime (coerce-valid-or-reject) so `Command(verb="memoryy", ...)` raises `ValueError` at construction, not just at routing. Static check + runtime check together close both the typo-survival and the untyped-caller bypass paths. See [3-4-t1-command-grammar-and-deterministic-parser.md](3-4-t1-command-grammar-and-deterministic-parser.md).
- **`BrainPort.delete_matching(target: str)` — opaque-reference privacy invariant unencoded.** Docstring requires `target` to be opaque (e.g., `"mode 'opaque'"`), never a raw app name. The `str` type cannot carry the "never raw" constraint; mypy accepts a window-title string. **Target: Story 5.2 (Selective Forget).** Wrap in `OpaqueTarget = NewType("OpaqueTarget", str)` at minimum; ideally a dataclass with a validator.
- **`BriefingAggregate` triple-nullable with no valid-state matrix.** `last_session: SessionSummary | None`, `last_snapshot: WorkspaceSnapshot | None`, `last_seed: str | None` — 2³ = 8 possible states; docstring doesn't document which are valid. **Target: Story 3.2 (BriefingAggregate population).** Either document the matrix or model as a tagged union (FirstRun / WarmResume / etc.).
- **`WindowContext.is_opaque` invariant unenforced.** Docstring promises `is_opaque=True ⇒ all three identity fields are None`, but `WindowContext(app_name="code.exe", is_opaque=True, ...)` constructs cleanly. **Target: Story 4.2 (Exclusion boundary at capture).** Add `__post_init__` validator asserting the implication.
- ~~**`ActionResult` tri-state `success`/`reason` has no invariant check.**~~ **Closed by Story 3.6 (2026-05-05).** `ActionResult.__post_init__` enforces the tri-state pairing: `success=True` requires `reason=None`; `success=False` requires a non-empty reason string. Programmer errors fail at construction with a `ValueError` rather than at the consumer. See [3-6-mode-restore-and-app-launching.md](3-6-mode-restore-and-app-launching.md).
- **Port docstrings cite absolute `architecture.md:NNN` line numbers.** Pervasive across 8 port files + 5 model files; any architecture.md edit shifts these silently. Today's cites are accurate. **Target: low-priority cleanup; revisit only if cites actually go stale.** If cleaning, replace with section-heading citations.
- **`test_ports_only_import_from_system_models_modules` accepts `segments[3] == "models"` but allows deeper nesting.** A future `nova.systems.brain.models.internal` would pass; today no such nesting exists. **Target: speculative — assert `len(segments) == 4` if the project ever introduces a nested `models/` package.**

---

## Deferred from: code review of story 2-2-api-key-configuration (2026-04-16)

- **AC #3 re-prompt wording split across two prints.** Spec quotes the empty-re-prompt as a single line `"No key entered. Type 'skip' to continue without cloud reasoning, or paste your key:"`. Implementation prints the instruction via `console.print(...)` then re-prompts with `"Enter your Anthropic API key: "` — two physical lines that carry equivalent information. Functionally correct; cosmetic deviation. **Target:** next UX-polish pass, OR the story that first centralizes wizard prompts into a shared helper (2.3 or 2.4).
- **`_FAILURE_MESSAGES.get(result, "Validation failed.")` is not exhaustive over `ValidationResult`.** If a future enum member is added (e.g. `OFFLINE`, `VERSION_MISMATCH`) without updating the dict, users see the generic fallback. **Target:** next test-hygiene pass — switch to `match`/`case` with `assert_never(result)` so mypy enforces coverage.
- **Integration tests mock `validate_api_key`.** All seven tests in `tests/integration/test_setup_wizard.py` patch the network-boundary function, so no real Anthropic request shape is exercised. **Target:** whichever story introduces `respx` or similar HTTP-mock infrastructure (likely when the Claude reasoning adapter lands in Epic 3). Fix: add one respx-backed test that exercises `_ping_anthropic` against a fake Anthropic response for each status class.
- **Missing `fsync` before `os.replace` in `settings_writer.write_api_key`.** `os.replace` is atomic at the filesystem metadata level but does not guarantee the temp file's data has reached disk. Power loss between the write and sync could land an empty/truncated `settings.yaml`. Acceptable v0.1 risk. **Target:** whichever story hardens the Brain storage write path (Story 5.5 SQLite corruption recovery would be a natural bundling point). Fix: `fh.flush(); os.fsync(fh.fileno())` before exiting the `with` block.
- **API key persisted as plaintext in `settings.yaml` — no secret storage.** Anyone with read access to the user profile (AV, OneDrive sync, malware) can read the key. No `os.chmod(0o600)` (Windows no-op), no Credential Manager / DPAPI usage. **Target:** T2 (v0.2) keyring migration. Fix: introduce a `SecretStore` port with a `keyring`-backed adapter; the setup wizard writes the secret there and `settings.yaml` holds a reference.
- **Transient errors consume the user-error retry budget.** `NETWORK_ERROR` and `SERVER_ERROR` each burn one of the 3 attempts. A user on flaky wifi times out after `3 × 15s = 45s` and hits the skip path with no "wait for connection" opportunity. **Target:** next UX pass on the wizard (likely in 2.4 when the full orchestration lands). Fix: separate budgets for typo-retry (AUTH_FAILED) vs transient-retry (NETWORK/SERVER) with a short backoff between transient attempts.
- **Re-prompt does not show "Attempt N of 3" progress.** Spec does not require this but absence is a UX gap — user has no feedback on how many tries remain. **Target:** 2.4 wizard polish. Fix: include `f"(attempt {attempt+1} of {_MAX_ATTEMPTS}): "` in the re-prompt string.

---

## Deferred from: code review of story 2-3-guided-mode-creation-wizard (2026-04-17)

- **`except BaseException` breadth in temp-file cleanup (`copy_template_verbatim` and `write_mode_file`).** Catches `SystemExit`, `KeyboardInterrupt`, and any other `BaseException` subclass solely to run `unlink(missing_ok=True)` on the temp path. The pattern is intentional — cleanup must run even on Ctrl+C — but the scope is wider than needed and matches the `settings_writer.py` pattern from Story 2.2. **Target:** a single cross-module polish PR that narrows both sites to `Exception` + `KeyboardInterrupt` together. Isolated change in Story 2.3 alone would create pattern drift.
- **Temp-file naming `{stem}.yaml.tmp` is not unique across concurrent wizard runs.** Two simultaneous `setup.bat` invocations (or a stale `.tmp` from a prior crash left behind on disk) could theoretically collide. The architecture contract is single-process single-user, so concurrency is explicitly out of scope for T1. **Target:** whichever story revisits write atomicity under concurrent-process assumptions (none currently planned). Fix: `tempfile.NamedTemporaryFile(dir=target.parent, delete=False)` with UUID suffix.
- **`resolve_app` preserves user-input casing for PATH-resolved apps.** When the registry misses and `shutil.which` resolves the name, the raw user input is stored verbatim as the `executable` field, not the canonical executable basename. This is documented in the `resolve_app` docstring as intentional ("preserving user input is the honest choice"). Potential inconsistency with `APP_REGISTRY` lookups which normalize to canonical lowercase. **Target:** revisit if Story 4.1 (Eyes Win32 context capture) surfaces executable-matching bugs traceable to casing drift.
- **NFKC Unicode normalization for mode names.** `"Café"` slugifies to `"caf"` (dropping the `é`); `"ﬁle"` (ligature U+FB01) slugifies to `""`; full-width digits `"１２３"` slugify to `""`. AC #7 explicitly specifies strict ASCII kebab-case, so this behavior is spec-conformant — but users may expect `"Café"` → `"cafe"`. **Target:** future UX-polish story, contingent on actual user feedback during the private-beta window. Fix: `unicodedata.normalize("NFKC", name)` before the existing lowercase + character-class filtering.
- **`copy_template_verbatim` parent-directory-exists precondition is not documented.** The function assumes `target_yaml.parent` exists; the production flow guarantees this via `modes_dir.mkdir(parents=True, exist_ok=True)` in `run_mode_wizard_step`, but a standalone caller would get `FileNotFoundError` from `shutil.copyfile`. **Target:** next doc-hygiene pass on `mode_wizard.py`. Fix: one-sentence docstring addition stating the caller owns parent-directory creation (matches the `settings_writer.py` "dumb I/O primitive" philosophy).

---

## Deferred from: story 2-4-briefing-card-state-a-initial-capture-and-setup-completion (2026-04-17)

- **Setup-time initial capture does not apply exclusion filtering.** `nova.setup.initial_capture.capture_initial_workspace` enumerates every visible top-level window and records raw app/process names without checking them against the user's exclusion list. The exclusion list loader + capture-layer enforcement are both Story 4.2 scope (Epic 4). For Story 2.4, raw window identities are stored only in the local `workspace_snapshots.workspace_data` row and never cross the cloud trust boundary until PromptBuilder (Story 4.6) is wired. **Target:** Story 4.2 — extend `_WorkspaceProbe` (or its Story-4.1 successor) to honor `ExclusionConfig` and emit `is_opaque=True` for matching windows; retrofit an integration test covering a setup-time capture of an excluded process.
- **Setup-time persistence writes directly through `SqliteStorageEngine` — not through `BrainPort`.** `persist_first_run` uses two raw `storage.execute(...)` calls (plus `execute_returning_lastrowid`) to INSERT the first `sessions` + `workspace_snapshots` rows, because Brain's adapter does not yet exist. This is documented in the module docstring as a setup-only seam. **Target:** Story 3.1 (Brain session & seed persistence). ✅ **Resolved by Story 3.1 (2026-04-21)** — `persist_first_run` now routes through `BrainPort.create_session(started_at=capture.snapshot.captured_at)` / `BrainPort.store_snapshot(WorkspaceSnapshotInput(...))` / `BrainPort.end_session(...)`; the three session/snapshot SQL constants and `_serialize_workspace_data` were removed from `initial_capture.py`. The `setup_complete` audit INSERT stays as direct SQL (`_INSERT_AUDIT_SQL`) because routing it through `AuditLogger` would re-introduce the observational-swallow-breaks-atomicity bug — see the comment on `_INSERT_AUDIT_SQL` for the rationale. `execute_returning_lastrowid` on the engine stays (Brain reuses it).
- **`_default_probe_factory` is typed as a bare `type` to admit test monkeypatching.** The intended annotation is `Callable[[], _WorkspaceProbe]`, but mypy strict rejects the test-side lambda reassignment. The runtime structural check at the capture boundary (`probe.available`, `probe.enumerate_hwnds()`, etc.) keeps type errors from leaking into production. **Target:** a focused typing-hygiene pass that introduces a `TypedProbeFactory` `Protocol` and narrows the module attribute accordingly.
- **No observability phase-timing breakdown in the persistence helper.** Story 2.4 AC #35 planned a DEBUG log with `phase` / `elapsed_ms` around each phase of `_run_initial_capture_and_persist` for future NFR2 regression diagnosis; implementation shipped with only the overall `< 8 s` timing assertion. Not blocking and easy to bolt on. **Target:** whichever story first needs to diagnose a setup-time slowdown. Fix: `logger.debug("setup phase complete", extra={"phase": "capture", "elapsed_ms": int((end-start)*1000)})` wrapping each phase.

---

## Deferred from: code review of story 2-4-briefing-card-state-a-initial-capture-and-setup-completion (2026-04-17)

- **Theoretical WAL/-shm race between fast-path probe and `create_app`.** `_probe_setup_complete` opens a `SqliteStorageEngine`, runs `SELECT 1 FROM audit_log`, closes; then `_run_initial_capture_and_persist` opens a second engine. Both are serialized via separate `asyncio.run` blocks so overlap is impossible in the current flow. **Target:** revisit if CI surfaces a sporadic `database is locked` error traceable to the probe; the fix would be to pass the same engine down into the capture+persist helper, or run the probe inline inside `_run_initial_capture_and_persist`.
- **80-column render test uses three short mode names, not the worst-case realistic list.** The current regression guard asserts `len(line) <= 80` against "coding, study, research" — never approaches the column limit. **Target:** a follow-up test-hygiene pass; add a test with 8 modes of 20-character names each to exercise the AC #21 upper bound honestly.
- **Engine `close()` in `finally` can mask the original `persist_first_run` exception.** Pre-existing pattern from `nova.core.storage.engine` Story 1.4 — `engine.close()` is documented as idempotent but could still raise on a corrupt WAL. If that exception surfaces after a `StorageError` from persist, the original failure is lost. **Target:** Story 8.x hardening pass across all composition-root teardown sites.
- **`load_config` non-`ConfigError` exception translation.** Story 2.4's `_run_initial_capture_and_persist` only catches `ConfigError`, trusting Story 1.6 to translate `OSError` / `UnicodeDecodeError` / `yaml.YAMLError` at its boundary. If that translation has any gaps, an unexpected exception class escapes to the asyncio loop and becomes a traceback. **Target:** a targeted audit of Story 1.6's exception boundary; not a Story 2.4 concern.
- **`create_app` non-`StorageError` exception translation.** Same pattern: `_run_initial_capture_and_persist` trusts `create_app` to surface only `StorageError` on init failures. `MigrationError` and unadorned `sqlite3.Error` subclasses should be translated inside `create_app`. **Target:** Story 1.10 boundary audit.
- **`app.close()` raising after `persist_first_run` raised.** The `finally: await app.close()` block can itself raise — we rely on engine close idempotency (Story 1.4) for safety. **Target:** Story 8.x hardening.
- **Wizard `KeyboardInterrupt` surfaces as a raw traceback.** The `run_api_key_step` / `run_mode_wizard_step` calls in `main()` aren't wrapped in a try/except — a Ctrl+C mid-wizard produces a Python traceback instead of a clean "Setup interrupted. Re-run setup.bat." message. **Target:** Stories 2.2 / 2.3 polish, or a cross-module signal-handler pass in Epic 8.
- **Two modes with identical casefolded `name` sort on dict insertion order.** A user who creates "Coding" and "coding" as separate modes gets a tie-break determined by `NovaConfig.modes` insertion order. **Target:** mode_wizard (Story 2.3) already normalizes stems; surface a clear rejection at `_is_valid_mode_stem` if a canonicalized-name collision would render indistinguishably.
- **`ModeConfig.name` whitespace-only.** Theoretically writable by hand-editing `%LOCALAPPDATA%/nova/modes/*.yaml`; Story 2.3's wizard rejects it at write time. **Target:** Story 1.6 config loader should reject `name: "   "` as well, for operator-edited mode files.
- **`test_storage_error_during_persistence_exits_one` mocks `persist_first_run` entirely.** The test therefore does not exercise the real `async with storage.transaction()` finally-close path. **Target:** integration test strengthening pass; replace the mock with a `StorageError` raised from a real `monkeypatch.setattr(engine, "execute", ...)` that still runs through the transaction context manager.
- **`test_transaction_rolls_back_on_snapshot_failure` uses a `StorageError` mock, not a real `IntegrityError`.** The engine's own rollback path is documented-but-not-regression-tested by Story 2.4. **Target:** add a sibling test that creates a `NOT NULL` violation on the snapshot INSERT so sqlite3 raises `IntegrityError` → engine translates to `StorageError` → `transaction()` ROLLs back.
- **`test_per_window_failure_is_graceful_partial` does not assert the HWND-only opacity.** The privacy contract ("log with HWND only, never the app/title/process") is locked in the module docstring but not the test. **Target:** assert `any(getattr(r, "hwnd", None) == 2 for r in caplog.records)` in a follow-up.
- **No regression guard asserting the capture-status line renders BEFORE the completion panel.** AC #19 specifies "directly before `create_app`" — the code honors it but no test locks the ordering. **Target:** add a simple stdout-capture test that asserts the capture-status substring precedes the `"Setup complete."` panel title in the recorded output.
- **`test_completion_panel_does_not_scan_filesystem` patches `Path.iterdir` / `Path.exists` at the class level.** Rich internals may call `Path.exists` for platform probing on some platforms; the global patch is a potential flake source. **Target:** narrow the patch to the specific `data_dir` / `modes_dir` instance, or assert non-invocation via `unittest.mock.patch.object` on a spec'd path.
- **`test_main_skips_api_key_when_localappdata_missing` does not assert State A rendered before the skip warning.** The flow is correct but no test captures the ordering invariant ("State A still renders when data_dir is None"). **Target:** extend the existing test to assert `"N.O.V.A."` appears in captured output before the `"LOCALAPPDATA not set"` string.

---

## Deferred from: story 2-5-api-key-update-post-setup (2026-04-17)

- **Skin-rendered amber Tier Notice panel on restart.** Story 2.5 ships the absent-key one-time notice as a plain `sys.stderr.write` line in `cli.py` because Skin doesn't exist yet. UX-DR10 amber styling + the rich Tier Notice component from ux-design-specification.md:815 will subsume it. **Target:** Story 5.4 (tier status display and notification). Fix: re-home `_emit_offline_notice_once` into the Skin Tier Notice component; delete the stderr write.
- **In-session `help` command printing the key-update path.** Story 2.5 documents the update path in `nova --help` epilog and `README.md`; the in-session `help` verb (which the T1 command vocabulary in ux-design-specification.md:860–948 reserves) does not yet exist. **Target:** Story 3.9 (status command and help display). Fix: when Story 3.9 wires the interactive `help` handler, pull the same `_HELP_EPILOG` constant (or its successor) and include the "edit settings.yaml" bullet so the CLI-time and session-time surfaces stay in sync.
- **Key migration to Windows Credential Manager / DPAPI.** The API key is persisted as plaintext in `%LOCALAPPDATA%/nova/settings.yaml`. Acceptable v0.1 risk because the data dir is per-user and `%LOCALAPPDATA%` is ACL-restricted, but AV / OneDrive sync / malware with user-level read can still see it. **Target:** T2 trust-hardening. Fix: introduce a `SecretStore` port with a `keyring`-backed adapter; the setup wizard writes to the credential store and `settings.yaml` holds only a reference.
- **`nova config` / `nova key rotate` subcommand.** Story 2.5 AC #1 fences this out as an explicit T2 candidate (epics.md:1043). The plain-text `settings.yaml` edit path remains the documented interface in T1. **Target:** T2 UX polish. Fix: add an `add_subparsers` group to `cli.py` guarded by a matching update to `tests/unit/test_cli_no_new_subcommands.py` (which intentionally blocks it today).
- **Boot-time cloud ping to proactively detect a revoked key.** Story 2.5 AC #24 intentionally skips this — a 15-second Claude ping on every `nova` invocation would violate Story 2.4's <8 s setup budget posture and add cost on hot paths. Nerve's opportunistic `check_now` before cloud-requiring operations (Story 3.5) is the correct detection surface. **Target:** Story 3.5 (nerve command routing and session lifecycle). Fix: wire `TierManager.check_now()` into the first real cloud-requiring command handler so a rotten key degrades to OFFLINE on first use, not on boot.

---

## Deferred from: code review of story 2-5-api-key-update-post-setup (2026-04-17)

- **`_clean_nova_logging.handler.close()` can raise `ValueError` on already-closed streams.** Fixture in both `tests/integration/test_api_key_update.py` and `tests/unit/test_cli_offline_notice.py`. Under `pytest-xdist` or `atexit`-ordering quirks, a `FileHandler` whose underlying stream has been closed will raise when the fixture's `handler.close()` runs. Pre-existing pattern from Story 1.10's test fixture — this is cross-test hygiene, not Story 2.5 specific. **Target:** a single cross-fixture polish pass that wraps `handler.close()` in `contextlib.suppress(ValueError)` across every `_clean_nova_logging`-shaped fixture.
- **`bluntness.value == "direct"` coupling in restart-integration test.** `tests/integration/test_api_key_update.py::test_settings_yaml_removed_key_is_picked_up_as_none` asserts `reloaded.settings.bluntness.value == "direct"` to prove non-key settings survive a key-removal rewrite. If `UserSettings.bluntness` ever becomes a plain string (or the enum's `.value` attribute name changes), the assertion breaks for a reason unrelated to Story 2.5's contract. **Target:** whichever story first refactors `UserSettings`. Fix: switch the assertion to use the enum member identity (`is BluntnessLevel.DIRECT`) or drop the non-key-field check and trust the dedicated config-loader tests to cover it.
- **Offline notice uses `\u26a0` without `\ufe0f` variation selector.** `src/nova/cli.py:_emit_offline_notice_once`. Most Windows terminals render bare `U+26A0` as monochrome text, not amber. The notice is still legible and correct; the "amber warning glyph" framing in docstrings and tests is aspirational. **Target:** Story 5.4 (Skin Tier Notice component) — when Skin owns the rendering, it can apply the variation selector or a Rich `[bold yellow]⚠[/bold yellow]` style consistently with the UX-DR10 color system.
- **`_emit_offline_notice_once` has no module-level idempotency guard despite the name.** The "once" semantics are enforced only by convention (a single call site at Step 6.5). If a future story adds a second call site by accident, the notice fires twice within the same `nova` invocation. **Target:** rename to `_emit_offline_notice` (drop the "once") or add a module-level `_emitted` flag with a reset hook for tests. Either is acceptable; pick whichever the next toucher of this file prefers.
- **No `try/finally` around `_async_main` in `test_stale_or_invalid_key_completes_bootstrap_without_exception`.** If the test body throws before the autouse fixture's teardown runs, the still-open `FileHandler` on `nova.log` holds a Windows file lock and `tmp_path` cleanup fails with `PermissionError`. Cross-test pattern concern — the same risk exists in every integration test that invokes `_async_main` directly. **Target:** a cross-integration-test hygiene pass that wraps the `_async_main` call in a `try/finally: logging.shutdown()` block so the file handle is released before `tmp_path` teardown.
- **`_clean_nova_logging` fixture only scans the root logger.** Handlers attached to named loggers (`nova.cli`, `nova.app`) are not cleaned between tests. If a future refactor attaches handlers via `logging.getLogger("nova")` instead of the root logger, the fixture silently leaks them across parallel tests. **Target:** a fixture-hygiene pass that iterates known nova logger names OR walks `logging.Logger.manager.loggerDict` and removes any handler whose name matches `_STDERR_HANDLER_NAME` / `_FILE_HANDLER_NAME` regardless of which logger it's attached to.
- **`test_initial_tier_is_offline_when_api_key_is_empty_string_after_load_config` doesn't seed `exclusions.yaml` or a mode file.** `tests/unit/test_app.py`. Today `load_config` accepts a minimal data dir with just `settings.yaml` + empty `modes/`, emitting a WARNING for "zero modes". The test passes because the WARNING isn't attached to assertion surface. If `load_config`'s zero-modes handling ever escalates to raise (cross-story policy tightening), the test flips from passing to `ConfigError`. **Target:** either seed a placeholder mode in this test to match the integration fixture pattern, or document the no-modes state as a stable invariant that survives future tightening.

---

## Deferred from: code review of story 3-1-brain-session-and-seed-persistence (2026-04-21)

- **`_INSERT_SESSION_SQL` uses magic NULLs in VALUES instead of omitting the columns.** `src/nova/adapters/sqlite/brain.py:91-93`. Style concern; future schema additions would trip tests rather than pass silently because mypy strict catches shape changes at usage sites. **Target:** next adapter-touch pass. Fix: `INSERT INTO sessions (started_at, mode_name, is_complete) VALUES (?, ?, 0)` relying on column defaults for nullable fields.
- **`get_last_session` / `get_last_seed` order by `id DESC` instead of a time-semantic column.** `src/nova/adapters/sqlite/brain.py`. Today's `AUTOINCREMENT` inserts are strictly monotonic so `id DESC` ≡ `started_at DESC`. If future crash-recovery or import tooling ever re-inserts historical rows (e.g., via a restore flow), the insert-order semantic diverges from the wall-clock semantic the docstrings imply. **Target:** whichever story introduces backfill (Story 5.6 backup/restore, or a post-T1 import tool). Fix: `ORDER BY COALESCE(ended_at, started_at) DESC, id DESC` or an explicit time column.
- **`get_last_snapshot_for_session` doesn't guard against `NULL` SQL values that become the string `"None"` via `str()`.** `src/nova/adapters/sqlite/brain.py:335-340`. Schema enforces `NOT NULL` on `captured_at` / `snapshot_type` / `workspace_data` today, so this is defensive-only. `str(None) == "None"` would cascade through `json.loads("None")` → `JSONDecodeError` → `StorageError`, or `SnapshotType("None")` → `ValueError` → `StorageError` — the safety net works accidentally. **Target:** Story 4.3 (JSON shape evolution) when the column-nullability contract is re-examined.
- **`WorkspaceSnapshotInput` doesn't validate `apps` entries are non-empty strings at construction.** `src/nova/systems/brain/models.py`. No current caller produces empty strings; validation is nice-to-have. **Target:** next DTO-hygiene pass, or the first story to accept user-controlled app names through a capture path (Story 4.1 win32 capture).
- **`end_session` second call on same `session_id` silently overwrites seed/summary/ended_at — no idempotency guard.** `src/nova/adapters/sqlite/brain.py:234-261`. UPDATE has no `WHERE is_complete = 0` filter. No current caller does double-calls; Story 3.7 (shutdown flow) will define the idempotency contract when it lands. **Target:** Story 3.7 (shutdown & seed capture).
- **`test_adapter_does_not_double_catch_storage_error_from_engine` parametrize mixes methods with different engine-call paths without asserting which path ran.** `tests/unit/adapters/sqlite/test_brain_adapter.py`. Test currently works correctly; concern is fragility under a future refactor that might make it pass vacuously for a method that no longer calls the monkeypatched engine entry point. **Target:** next test-hygiene pass. Fix: split into per-method tests and spy on the expected engine method.

## Deferred from: code review of story 3-2-briefingaggregate-and-state-determination (2026-04-22)

- **Index on `sessions(mode_name)` not added.** `src/nova/core/storage/migrations/`. Story 3.2's `SqliteBrainAdapter.get_mode_last_used` performs `SELECT … WHERE mode_name = ? LIMIT 1` without a dedicated index — each per-mode lookup is a sequential scan. At T1 scale (1–5 modes × ≤ ~1,000 session rows over 6 months), well under NFR budget. Becomes a bottleneck at 50+ modes or multi-year session history. **Target:** whichever story first ships at T2 scale, or a documented perf regression. Fix: add `CREATE INDEX idx_sessions_mode_name ON sessions(mode_name)` via a new numbered migration.
- **`SqliteBrainAdapter.get_mode_last_used` does not validate returned `started_at` as ISO-8601.** `src/nova/adapters/sqlite/brain.py`. Adapter returns `str(row["started_at"])` raw; if upstream writers ever insert a non-ISO string or a native datetime (which `str()` stringifies to a non-ISO format), the render layer's `datetime.fromisoformat` raises deep in UI code instead of failing at the port boundary. Today all writers go through `events._utc_now_iso()` so the contract holds. **Target:** whichever story first introduces a new writer of `sessions.started_at` outside the canonical clock, or a general adapter-boundary validation pass. Intentionally NOT added in Story 3.2 because "adapters translate, never decide" (project-context.md §Architecture) — defensive validation here would duplicate the upstream writer's contract.
- **`ModeInfo.is_default` tie-break not enforced.** `src/nova/systems/brain/models.py`. Multiple modes can set `is_default=true` in their YAML; the config loader logs a warning but passes all flagged modes through. Story 3.2 propagates the flag verbatim into each `ModeInfo`; no invariant test, no `__post_init__` validation, no assembly-time warning. **Target:** Story 3.3 (BriefingViewModel assembly) or Story 3.5 (Nerve session lifecycle) — whichever first consumes `ModeInfo.is_default` for a concrete decision (e.g., suggested-mode fallback). Fix: add a tie-break rule at the consumption site (alphabetical? first-in-sort?) and log a WARNING at aggregate assembly when >1 default mode exists.
- **No snapshot-read isolation during concurrent writes to `load_briefing_aggregate`.** `src/nova/systems/nerve/briefing.py`. The assembler issues 3+N sequential reads on the shared `SqliteStorageEngine` without wrapping them in a transaction. If a concurrent coroutine writes to `sessions` between reads, the aggregate sees read-your-writes-inconsistent data. In T1 (single-user, single-process, briefing-assembly runs on bare-boot with no concurrent session activity) this is unreachable; Story 3.5 will establish whether briefing assembly runs inside a read transaction. **Target:** Story 3.5 (Nerve session lifecycle). Fix options: (a) wrap `load_briefing_aggregate` body in `async with storage.transaction():`, (b) document single-reader invariant in Nerve's lifecycle contract.

- **Hardcoded BrainPort method lists drift silently across three sites.** `tests/unit/adapters/sqlite/test_brain_adapter.py::test_sqlite_brain_adapter_structurally_satisfies_brainport`, `tests/unit/systems/nerve/test_briefing_assembly.py::test_recording_fake_implements_all_brainport_methods`, and `tests/unit/ports/test_port_isolation.py::PORT_CONTRACT[brain_port_module]` all enumerate the BrainPort method set by hand. Adding a new port method requires updating three hand-maintained lists; missing any one gives false-pass coverage. Story 3.2 caught one such drift (adapter conformance list missed `get_mode_last_used`). **Target:** whichever story next extends `BrainPort`, or a dedicated test-hygiene pass. Fix: introduce a shared `BRAIN_PORT_METHODS` constant (pulled from `PORT_CONTRACT` at module scope) and have the two runtime guards iterate it. Reduces three drift sites to one.

- **`test_setup_bat.py` deadlocks under pytest default capture on Windows.** `tests/integration/test_setup_bat.py`. The four tests in this file invoke `subprocess.run([str(SETUP_BAT)], capture_output=True, ...)`, which on Windows triggers a pipe-handle-inheritance deadlock: cmd.exe → uv.exe → python.exe grandchildren inherit pytest's capture file handles and hold them open, so pytest's end-of-test pipe drain hangs indefinitely. Workaround: run `pytest tests/integration/test_setup_bat.py -s` (disables capture). Diagnosed during Story 3.2 code-review verification — was previously masked because earlier story closures ran the full suite without hitting the new Defender-validation timing window. **Target:** test-hygiene pass on Story 2.1's integration suite, OR whichever Win11 dev-experience story lands next. Fix options: (a) replace `subprocess.run(capture_output=True)` with manual Popen + `creationflags=CREATE_NEW_PROCESS_GROUP` + explicit `close_fds=True` and pipe-handle hygiene, (b) wrap the test in a fixture that scopes `-s` for that file only, (c) refactor the test to run setup.bat fully detached and only assert on filesystem state. Until fixed, the canonical Windows local-dev invocation is `pytest --ignore=tests/integration/test_setup_bat.py` + `pytest tests/integration/test_setup_bat.py -s` as two passes.



## Deferred from: code review of story-3.3 (2026-05-04)

- **Lexicographic ISO timestamp comparison breaks across non-UTC offsets.** `_pick_recent_or_default` in `src/nova/systems/ritual/system.py` does a lexicographic max over `last_used_at` strings. Today all writes are `+00:00` (Story 3.1 lock), so this is moot. **Target:** T2 multi-machine sync, OR whichever story relaxes the UTC-only writer constraint. Fix: parse to `datetime.fromisoformat(...)` and compare canonical UTC instants.
- **Lexicographic ISO comparison breaks across millisecond/second precision.** Same site as above. Codebase emits no fractional seconds today, but `datetime.fromisoformat` accepts them and a future precision bump in `_utc_now_iso` would silently change tie-break outcomes. **Target:** revisit if any future story changes timestamp precision.
- **Comma in `WindowContext.app_name` produces ambiguous "Apps:" list output.** `_build_last_apps_label` does `', '.join(app_names)`; an app named `"Acme, Inc. Browser"` looks like two entries. **Target:** Story 4.2 (capture-layer exclusion + name normalization) owns app-name sanitization upstream of Ritual.
- **Newline character in mode `display_name` breaks the block-spacing model.** A YAML literal-block scalar `name:` produces a multi-line `display_name` that the renderer treats as one block, splitting the prompt visually. **Target:** whichever story tightens `nova.core.config` mode-name validation (likely Story 1.6 follow-up or Story 6.4 ad-hoc mode editing).
- **Corrupt session row with `duration_seconds=0` is indistinguishable from a legitimately 0-second completed session.** `brain.py:_compute_duration_seconds` clamps parse errors to 0 + WARNING log; Ritual then renders `"Last session: Coding mode, 0s"`. The user cannot tell DB-corruption from "you closed it instantly." **Target:** Story 5.5 (SQLite corruption recovery flow) — surface a distinct UX path for parse-error rows.
- **No regression test for "duplicate apps render verbatim" current behavior.** `_build_last_apps_label` does not deduplicate; two Chrome windows render as `"Apps: Chrome, Chrome"`. The dedup decision is itself deferred (per Story 3.3 spec) — but no test currently locks the verbose behavior, so a silent dedup change would not fail. **Target:** whichever story first ships dedup (likely Story 3.6 mode-restore feedback).
- **Degenerate State C: empty `available_modes` with a populated seed.** The user previously had a coding mode, captured a seed, then deleted the mode YAML. State C reaches `suggested_mode=None`, prompt falls back to "What mode?" — no breadcrumb explains the missing mode context to the user. **Target:** Story 3.10 (crash recovery / unexpected termination) or a future UX-polish story. Fix options: (a) detect-and-warn render path, (b) preserve display_name on seed write, (c) accept silent omission as the spec intent.

---

## Deferred from: code review of 3-4-t1-command-grammar-and-deterministic-parser (2026-05-05)

- **Smart quotes / Unicode punctuation in NL phrases produce UNKNOWN.** Curly apostrophe (`'`) in `"what's my status"` doesn't match the literal-form NL phrase `"what's my status"` (straight apostrophe). Common terminal autocorrect / paste-from-iMessage input falls to UNKNOWN. **Target:** future UX-polish story once user feedback indicates the gap is hit. Fix options: (a) `unicodedata.normalize("NFKC", phrase)` in the lookup pipeline, (b) explicit literal alias entries for each curly variant, (c) accept as out-of-scope and document in user help.
- **Trailing punctuation on contextual reply (`yes!`, `no.`, `cancel?`) routes to UNKNOWN.** Layer C single-token discipline (Spec 3.4 AC #9) is whole-input match against the lowercased single token; `yes!` is one token but its value is `"yes!"`, not in `_CONTEXTUAL_REPLIES`. Common natural-language confirmation lost. **Target:** future UX-polish story. Fix: pre-strip trailing `.,!?;:` from the single-token contextual-reply candidate before lookup.
- **`Command` pickle/unpickle bypasses `__post_init__` validation.** Pickled Commands restored via `__setstate__` skip `__post_init__` (the `from err` chain inside `__post_init__` only fires during the dataclass `__init__` path). No consumer pickles Commands today (Brain stores text, not pickles), so this is theoretical. **Target:** whichever story first introduces a Command persistence path. Fix: add `def __setstate__(self, state): self.__dict__.update(state); self.__post_init__()` at that time.

## Deferred from: code review of story 3-5-nerve-command-routing-and-session-lifecycle (2026-05-05)

- **POSIX signal-handler branches uncovered (`pragma: no cover`).** [src/nova/systems/nerve/system.py:_install + _uninstall POSIX branches] — project is Windows-only per project-context.md. **Target:** the eventual cross-platform / Linux-support story. Add unit tests that monkeypatch `sys.platform` to "linux" and stub `loop.add_signal_handler` to assert the calls happen with the right signals.
- **`_install_signal_handler` propagates `ValueError` if `NerveSystem.startup` runs in non-main thread.** [src/nova/systems/nerve/system.py:_install_signal_handler] — `signal.signal` raises `ValueError("signal only works in main thread")` outside the main thread; uncaught. Story 3.5's contract is one `startup` per process via `cli.py` (main-thread only). **Target:** an embedding-API story that lets external tools run NerveSystem in a worker thread. Fix: wrap signal install in try/except and degrade to "no signal handler installed" with a WARNING log.
- **`_signal_handler_task` is not cleared after handler completes — cleanup contract is asymmetric.** [src/nova/systems/nerve/system.py:_signal_handler_task semantics] — cleanup checks "task exists" not "task ran successfully". No real trigger today (`_shutdown_event` is never reset). **Target:** whichever future story supports `startup` re-entry semantics. Fix: clear `_signal_handler_task = None` once the handler completes (via `task.add_done_callback`), OR have cleanup additionally inspect `task.exception()`.
- **`collect_input` daemon thread leaks across REPL iterations.** [src/nova/adapters/rich/skin.py:collect_input] — every REPL turn spawns a fresh `nova-skin-input` daemon thread; cancelled losers keep running with `Prompt.ask` blocked on stdin. Bounded in 3.5 (REPL exits unconditionally on shutdown_event). **Target:** whichever future story adds REPL re-entry or persistent-session semantics. Fix options: (a) reuse a single thread per `NerveSystem` instance, (b) interrupt the underlying stdin read via `sys.stdin.close()` on cancel, (c) accept the orphan and document the bound.
- **`_cleanup_after_repl` Phase 1 `contextlib.suppress(Exception)` lets `CancelledError` propagate.** [src/nova/systems/nerve/system.py:_cleanup_after_repl Phase 1] — if the handler task is cancelled by loop teardown, the CancelledError propagates past suppress (it's BaseException) and replaces whatever exception triggered the finally. Acceptable production behavior. **Target:** revisit if test flakiness emerges. Fix would be to wrap with `contextlib.suppress(Exception, asyncio.CancelledError)` only for the cleanup-await context, but that risks silencing real cancellation.
- **State A path render exception is fatal — no degraded fallback.** [src/nova/systems/nerve/system.py:startup step 6] — a single error at the briefing-build boundary aborts the bare-`nova` invocation entirely with a traceback. Acceptable per the State A contract. **Target:** future UX-polish story. Fix: wrap the State A render in try/except, log + emit a minimal text fallback ("Welcome to N.O.V.A. Run `setup.bat` to configure your first mode.") so the user sees something useful even on a render failure.

## Deferred from: code review of story 3-6-mode-restore-and-app-launching (2026-05-05)

- **`subprocess.Popen` handle held until GC; not explicitly waited or closed.** [src/nova/adapters/win32/actions.py:_popen_launch] — DETACHED_PROCESS releases console / process-group inheritance but the parent `Popen` wrapper still holds a `_handle` until garbage collection. Negligible for T1 modes (≤6 apps per restore, infrequent invocation), but a long-running session that switches modes hundreds of times accumulates handles. **Target:** future Story 6.1 (window focus + arrange) when Hands grows additional Popen-driven entry points. Fix options: (a) explicit `proc.poll()` / weakref tracking + periodic reap; (b) use a `Popen.__exit__` context manager pattern.
- **`asyncio.wait_for` timeout cancels the asyncio task but not the underlying `to_thread` thread.** [src/nova/adapters/win32/actions.py:launch_app phase 2] — when the timeout fires, the wrapper coroutine returns `REASON_TIMED_OUT`, but `subprocess.Popen` may still spawn the process moments later. Caller is told "not launched" while the OS spawns it anyway. **Target:** a follow-up story that introduces a real "verified launch" contract (e.g., wait for process to enter Win32 message loop). Fix: document the contract as "we gave up waiting; the OS may still spawn"; consider pre-checking PID after timeout.
- **`os.startfile` fallback NOT triggered for `.lnk` shortcuts when args are present.** [src/nova/adapters/win32/actions.py:launch_app phase 3] — args-empty gate skips startfile when `app.args != ()`, so a Windows mode YAML with `executable: chrome.lnk args: [--new-window]` fails with `REASON_NOT_FOUND` instead of routing to ShellExecute (which would silently drop the args — also bad). **Target:** Story 6.5 (URL opening, full mode configuration) or whichever story first encounters the `.lnk + args` pattern. Fix options: (a) detect `.lnk` extension and route through `os.startfile` with `arguments=` (Python 3.10+) accepting the inconsistent semantics with a documented caveat; (b) reject `.lnk + args` at the loader with a clear error.
- **Extended Win32 winerror codes for "permission" not mapped (`740` ELEVATION_REQUIRED, `1314` PRIVILEGE_NOT_HELD).** [src/nova/adapters/win32/actions.py:_map_os_error] — `winerror=5` correctly maps to `REASON_PERMISSION_DENIED`, but UAC-elevation-required (740) and missing-privilege (1314) fall through to `REASON_UNKNOWN_ERROR`. User sees "unknown error" instead of the actionable "permission denied" hint. **Target:** test-hygiene pass / next adapter touch. Fix: extend the `_map_os_error` permission-bucket check to include winerrors 740 and 1314.
- **Per-app audit `details["executable"]` carries the user's full filesystem path.** [src/nova/systems/hands/system.py:per-app log_action] — for `executable: "C:\\Users\\sayuj\\AppData\\Local\\Foo\\foo.exe"` the audit row's JSON details persists the full path. T1 is single-user single-machine so the privacy concern is theoretical, but a future user shipping `nova.db` to an issue tracker (or to the eventual transparency export) leaks local install layout. **Target:** Epic 5 (Transparency / Forget) when the audit-trail user-export path lands. Fix: store basename only (or hash) in audit; full path stays in the WARNING log if needed for diagnosis.
- **Whitespace-only `app.executable` reaches Popen and produces a misleading "is it installed?" hint.** [src/nova/core/config.py:AppConfig has no validator] — `AppConfig(name="X", executable="   ")` survives the loader and Popen raises `FileNotFoundError`, mapping to `REASON_NOT_FOUND` with the "is it installed?" hint — the real problem is config malformedness. **Target:** Story 6.4 (mode editing) — when the editor adds validation. Fix: extend the Story 1.6 loader's `_validate_app_entry` to reject empty/whitespace executables with a warning + drop the entry.
- **No concurrent-restore lock on `HandsSystem`.** [src/nova/systems/hands/system.py:HandsSystem] — Nerve's REPL is single-threaded so user-typed `mode A; mode B` serializes naturally; if a future automation calls `restore_mode` from a background task (e.g., Story 4.x scheduled mode switch) while a user-initiated one is in flight, per-app audit / render / event lines from both interleave on stdout. **Target:** whichever future story introduces non-REPL HandsPort callers. Fix: add an `asyncio.Lock` field on `HandsSystem`; document "single concurrent restore" as the contract.
- **psutil `process_iter` is not bounded — large modes (50+ apps) trigger O(N) sequential scans.** [src/nova/adapters/win32/actions.py:_is_already_running] — typical 3–6 app modes are unaffected. A 100-app mode iterates the full process table 100 times (~30s on a busy system). **Target:** Story 6.3 (ad-hoc mode creation) if it lifts the per-mode app cap, or first user report. Fix: snapshot `psutil.process_iter` ONCE at start of `restore_mode` and pass an already-running set into the launcher.
- **`_active_mode_name` not cleared by signal handler.** [src/nova/systems/nerve/system.py:_signal_handler_callback] — Story 3.7 will reset on clean shutdown, but a Ctrl-C between `mode coding` and the user typing `shutdown` leaves the field set. No observable effect today (process exits next), but a future story adding `startup()` re-entry would inherit a stale value. **Target:** whichever story introduces re-entry. Fix: include `self._active_mode_name = None` in the `startup()` step 1 lifecycle reset (NerveSystem already resets `_session_id` / `_session_active` / `_prompt_context` there).
- **`_summary_text` "1 more skipped" is grammatically odd.** [src/nova/systems/hands/system.py:_summary_text] — 3 apps, 2 fail → final line ends `"(1 more skipped — see status for details.)"`. "1 more skipped" reads awkward; "1 other skipped" or "1 also skipped" reads better. Cosmetic. **Target:** Voice / personality polish (Epic 7). Fix: pluralization helper or rephrasing.
- **`_summary_text` "Workspace ready." gives no acknowledgment of WHICH workspace.** [src/nova/systems/hands/system.py:_summary_text] — multi-mode users lose context (which mode just restored?). Voice (Epic 7) is expected to dress the line into "Coding workspace ready. Last thread: auth tests." **Target:** Epic 7 Voice prose enrichment. Fix: prepend `mode_config.name` to the summary, OR let Voice replace the bare line with a personality-bearing version.
- **Two N.O.V.A. instances launching the same mode simultaneously double-launch.** [src/nova/adapters/win32/actions.py:_iter_processes_for_match] — the already-running pre-check is non-atomic; two `nova.exe` processes both check, both find nothing, both `Popen`. Documented in the spec as best-effort; flagging here for clarity. **Target:** unlikely production concern (single-user product); revisit if user reports duplicate windows. Fix: process-wide named-mutex around the launch sequence.
- **Markup-injection regression test for `render_progress` not added.** [tests/unit/adapters/rich/test_skin_adapter.py] — `markup=False` is asserted, but no test passes a target like `"[bold]app[/]"` to lock that the literal `[bold]` characters render rather than activating Rich markup. The `markup=False` kwarg lock catches the regression class indirectly. **Target:** test-hygiene pass. Fix: add one parametrized test asserting that `ActionResult(target="[bold]X[/]", ...)` renders the literal brackets.
- **Spec internal cleanup — Story 3.6 spec file has stale text.** [_bmad-output/implementation-artifacts/3-6-mode-restore-and-app-launching.md] — three minor inconsistencies the implementation correctly resolved against: (a) AC #3 step 6 references `details["already_running"]` but AC #7 step 4 + Story 1.8 reliance table say the no-op-launch fact lives in DEBUG log only; (b) AC #3 step 2 says `psutil.process_iter(["name", "exe"])` but impl uses `["name"]` only — functionally equivalent and matches the basename-match strategy; (c) Task 1 sub-bullet still lists `REASON_ALREADY_RUNNING` in the `__all__` literal even though the binding ACs (#13, #3 step 2) correctly excluded it. **Target:** spec polish before next `create-story` run touches this file. Fix: edit the spec to align with the impl decisions (the impl IS correct — the spec is stale).

## Deferred from: code review of story 3-6-mode-restore-and-app-launching (2026-05-05, formal /bmad-code-review pass)

- **Mid-loop exception in `restore_mode` leaves orphan per-app audit rows + no aggregate `MODE_RESTORE` row + no `ModeRestored` event.** [src/nova/systems/hands/system.py:restore_mode] — Two trigger paths: (a) a programmer error from `audit.log_action` (`TypeError` / `ValueError` per Story 1.8 boundary checks) raised on the SECOND app's per-app audit row leaves the FIRST app's launch tracked but the aggregate fan-out skipped; (b) `asyncio.CancelledError` mid-loop has the same shape. Story 4.1's snapshot trigger (subscribed to `ModeRestored`) silently misses the mode boundary; Story 5.3 transparency reads will show orphan per-app rows with no aggregate. **Acceptable per AC #19**: the unwrapped-audit contract is intentional (programmer errors must surface). Softening would require a NerveSystem-layer design decision: should a mode-restore exception kill the session (current — `cli.py` maps to `EXIT_UNEXPECTED=4`) or render a friendly error and continue? **Target:** Story 3.7 (Shutdown Flow) or Story 3.10 (Crash Recovery) — both are natural homes for the cleanup-on-mid-loop-exit invariant. Fix options: (a) wrap Steps 5–7 in `try/finally` and emit a `result="cancelled"` aggregate audit row on exception (breaks the AC #19 unwrapped-audit promise); (b) catch `Exception` at `NerveSystem._handle_mode_switch` and render a friendly "command failed: <reason>" line; (c) accept current behavior + document the audit-trail-orphan invariant.
- **`_normalize_exe_basename` only strips a single trailing `.exe`.** [src/nova/adapters/win32/actions.py:_normalize_exe_basename] — Compound suffixes like `chrome.exe.bak` produce normalized basename `"chrome.exe.bak"` (no strip), which never matches a running `chrome.exe` (normalized `"chrome"`). Theoretical pattern; no real Windows app uses `.exe.bak` as a launch target. **Target:** test-hygiene pass / next adapter touch. Fix options: (a) document the single-strip semantic in the function docstring; (b) use `PurePath(value).stem.lower()` to drop ANY single extension (changes behavior for `.bat` / `.cmd` / `.lnk` matching too — needs a deliberate decision).
- **URL-deferral INFO log fires every `restore_mode` call.** [src/nova/systems/hands/system.py:restore_mode step 2] — A user repeatedly restoring a mode with URLs (Story 6.5 will eventually open them) sees one INFO line per restore in nova.log. Cosmetic log noise. **Target:** test-hygiene pass OR Story 6.5 (where the deferral becomes obsolete). Fix options: (a) drop to DEBUG; (b) gate behind a per-instance `seen_url_modes: set[str]` so each mode's deferral notice fires once per process lifetime.
- **`render_progress` doesn't sanitize ANSI / VT100 escape sequences in `result.target`.** [src/nova/adapters/rich/skin.py:render_progress] — `markup=False` blocks Rich `[bold]…[/]` markup but does NOT strip raw `\x1b[…]m` ANSI sequences or carriage returns. A user-written YAML with `name: "Bad\x1b[2J"` (clear-screen) would be passed verbatim through `console.print` and clear the user's terminal mid-launch. Requires malicious user-written YAML — limited threat model in T1 single-user product. **Target:** Story 6.4 (mode editing) when the editor adds AppConfig.name validation. Fix: extend the Story 1.6 loader's `_validate_app_entry` to strip `\x00–\x1f` (excluding tab) from `name`.
- **No kebab-case validator precondition on `restore_mode(mode_stem, ...)`.** [src/nova/systems/hands/system.py:restore_mode step 1] — Existing assert rejects empty/whitespace `mode_stem`, but doesn't validate kebab-case. Today the only call site (`NerveSystem._handle_mode_switch`) passes `command.target` which the Story 3.4 parser lowercases to identifier-shape. A future caller (Story 3.8 resume path, Story 6.3 auto-switch-after-create) could accidentally pass an arbitrary string into the canonical-identity slot — flowing through to `audit.target` and `ModeRestored.mode_name`. **Target:** Story 6.3 (ad-hoc mode creation) or first caller that needs the seam. Fix: `assert re.fullmatch(r"[a-z][a-z0-9-]*", mode_stem)` at top of `restore_mode`.
- **Audit-log row payload size for large modes.** [src/nova/systems/hands/system.py:aggregate audit row] — `details["apps_launched"]` and `details["apps_failed"]` carry full `list[str]`; for the deferred 50-app mode case (~20 char names + JSON overhead = ~2KB per row) the row is well within SQLite TEXT limits but accumulates fast under heavy mode-switch frequency. **Target:** bundle with the existing "Large-mode (50+ apps) psutil scan unbounded" deferred entry — both fire on the same trigger condition. Fix when that lands: cap the per-list size in `details` (e.g., first 20 + `"...N more"` summary) OR persist the full lists as a separate normalized table.
