# generated: 2026-04-14
# Single `last_updated:` comment below mirrors the live `last_updated:` field on line ~38;
# the field is the source of truth. Keep only the most recent change summary here — prior
# epochs live in git history, not in stacked comments.
# project: AI Assistant
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-04-14
last_updated: 2026-05-05 (Story 3.6 done after formal /bmad-code-review pass + user-reported HIGH post-review fix. **User-reported HIGH (caught by user after the 3-layer review missed it):** Story 3.4 contract regression — `_handle_mode_switch` used `command.target` verbatim as the `NovaConfig.modes` dict key, breaking the case-insensitive-lookup contract Story 3.4 line 406 spelled out (parser preserves casing in target; Nerve does case-insensitive lookup). User-typed `mode Coding` or `Switch to Coding mode` → parser produces `target="Coding"` → my lookup missed the canonical lowercase `"coding"` key → wrong "No mode named 'Coding'" error instead of restoring the mode. **Fix:** `mode_stem = command.target.lower()` for canonical lookup + downstream identity (`hands.restore_mode`, `_active_mode_name`, audit `target`, `ModeRestored.mode_name`); error template echoes `command.target` (original casing) so the user recognizes their input. Locked by 2 new tests: (a) `test_mode_switch_lookup_is_case_insensitive_and_passes_lowercased_stem_to_hands` asserts `target="Coding"` resolves to `hands.restore_mode("coding", config.modes["coding"])`; (b) `test_mode_switch_unknown_target_error_echoes_original_user_casing` asserts `target="UnknownMode"` renders `"No mode named 'UnknownMode'"` (original casing in error). 1863 unit pass + 56 integration pass; 100% coverage on Story 3.6 modules holds. Original /bmad-code-review entry: pass (3 fresh-context layers — 3 fresh-context review layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) ran; 18 raw findings → 13 unique post-dedup; 0 decision-needed, 6 patches applied + locked by new tests, 6 deferred with explicit deferred-work.md entries, 1 dismissed. **Patches:** psutil per-proc except widened to (NoSuchProcess, AccessDenied, ZombieProcess, OSError) so a single zombie process doesn't abort the entire iter; empty-target guard in _iter_processes_for_match prevents empty/`.exe` inputs from matching empty-named processes; ActionRequest.__post_init__ MappingProxyType passthrough dropped — always re-wraps via dict(...) copy so caller-retained source dict can't smuggle mutations; _active_mode_name now cleared (else-branch) on second-restore total-failure so Story 3.7 shutdown summary doesn't lie about a previously-active mode; AST guard test_restore_mode_does_not_wrap_audit_log_action_in_try_except now walks body+orelse+finalbody+handler bodies (was body only); new parametrized test_restore_mode_propagates_cancelled_error_from_isolated_surfaces over (render_progress, per_app_emit, render_response) locks `except Exception` (not BaseException) propagation contract. Final: 1861 unit pass (+7 vs post-impl baseline) + 56 integration pass; 100% coverage holds on all six Story 3.6 modules (nova.ports.app_launcher, nova.adapters.win32.actions, nova.systems.hands.{__init__,models,system}); ruff + ruff format + mypy strict all clean. Story 3.6 implementation complete; Status: done. 1854 unit pass (+106 net) + 56 integration pass (+2 windows_only Notepad-driven mode-restore tests); 100% coverage on every Story 3.6 module (nova.ports.app_launcher, nova.adapters.win32.actions, nova.systems.hands.{models,system}); ruff + ruff format + mypy strict all clean. Same-session adversarial review (3 layers in parallel general-purpose subagents — Blind Hunter / Edge Case Hunter / Acceptance Auditor) ran AFTER implementation; 7 patches applied in same session: (1) skin/event isolation in HandsSystem so a UnicodeEncodeError or BrokenPipeError on render does NOT abort the per-app loop and skip the aggregate MODE_RESTORE audit + ModeRestored event, (2) _active_mode_name set only when at least one app launched (zero-launches → stays None so Story 3.7 shutdown summary doesn't lie), (3) psutil basename normalization (chrome config matches chrome.exe running via new _normalize_exe_basename helper that strips .exe and lowercases), (4) psutil.Error broader catch in the iter (rare resource-exhaustion paths fall through cleanly to "not running"), (5) os.startfile POSIX hasattr guard (defensive — production stays Windows-only), (6) cross-mock chronological ordering tests via parent.attach_mock pattern (per-app + aggregate), (7) per-app audit details shape test asserting {"executable", "reason"} for both success and failure rows. 13 follow-ups deferred with explicit deferred-work.md entries (Popen handle leak; wait_for+Popen race; .lnk+args fallback; extended permission winerrors; executable-path PII in audit; whitespace-executable validation; concurrent restore lock; large-mode psutil scan; Ctrl-C _active_mode_name cleanup; "1 more skipped" plural; workspace name in summary; two-instance race; markup-injection regression test; spec stale-text cleanup). Original Story 3.6 spec: created at ready-for-dev: Mode Restore & App Launching. **Spec revised post-review** to fix five blockers: (1) stem-vs-display-name drift — HandsPort.restore_mode reshaped to (mode_stem: str, mode_config: ModeConfig); ModeRestored.mode_name + audit MODE_RESTORE target + _active_mode_name + "mode edit <stem>" hint all use stem; (2) already-running treated as success=True/reason=None (not failure) so all-already-running isn't misreported as total failure — adapter logs no-op-launch fact at DEBUG; (3) audit-failure isolation tests use real AuditLogger over a tiny in-test _FailingStorageEngine so AuditLogger's internal StorageError swallow runs (patching log_action would contradict the unwrapped contract) + AST guard locks "no try/except around log_action"; (4) Win32 adapter unit tests now platform-neutral via mocking (subprocess.Popen, os.startfile, psutil — no @windows_only) so the 100% coverage gate works on any runner; @windows_only kept only on the 2 real-Notepad integration tests; (5) os.startfile fallback gated on args==() so user's args contract is trustworthy. Closed-set canonical reason vocabulary now four members (not five): not found / permission denied / timed out / unknown error. New AppLauncherPort + Win32HandsAdapter + HandsSystem (sequential per-app loop with audit→render→event ordering, graceful-partial pattern, three distinct final-line summaries). NerveSystem._handle_mode_switch body replaced with HandsPort delegation; positive lock via PropertyMock that mode restore does NOT consult tier_manager. SkinPort.render_progress signature reshapes from Sequence[ActionResult] to single ActionResult per call. ActionResult/ActionRequest gain __post_init__ validators (closes deferred-work.md:137 + :146). 38 ACs across 14 groups (A-N); ~45-60 new unit tests + 2 windows_only integration tests planned. Original Story 3.5 entry: done with post-review HIGH fix. **User-reported HIGH:** Ctrl-C exited with code 0 instead of EXIT_INTERRUPTED=130 because the custom signal handler suppressed KbdInt propagation; `startup()` returned normally → `_async_main` returned EXIT_OK. **Fix:** `startup()` now raises `KeyboardInterrupt("session interrupted by signal")` after the finally block when `_signal_handler_task is not None` (signal-driven exit); SHUTDOWN command and EOF paths leave the field None and return EXIT_OK as before. Locked by `test_startup_raises_keyboard_interrupt_when_signal_handler_ran` (sync `asyncio.run` + sync `pytest.raises` to dodge pytest-asyncio's BaseException trap), 2 negative-case unit tests, and integration test `test_cli_keyboard_interrupt_from_nerve_startup_returns_exit_interrupted` (cli.py end-to-end). 1800 tests pass; 99.7% coverage on system.py.) Original review entry: review → done. Same-session three-layer adversarial review (Blind Hunter / Edge Case Hunter / Acceptance Auditor) ran in parallel general-purpose subagents: 37 raw findings, 34 unique post-dedup. Triage: 5 decision-needed (all resolved) + 13 patches (all applied) + 6 deferred (logged in deferred-work.md) + 10 dismissed. Decisions accepted: AST-only KbdInt test, AST composition-root helper, no-timeout on `_handle_shutdown` (documented why), best-effort-once cleanup, drains-pending no-warning test added. Patches: HIGH `_handle_shutdown` duplicate-write fix (flip `_session_active=False` BEFORE await + try/except), HIGH `_run_repl` outer CancelledError drain, HIGH `_should_skip_briefing` `(ValueError, TypeError)` catch, MED `render_response` markup=False, MED cleanup `wait_for(handler_task, timeout=3.0)`, MED EOF/KbdInt sentinel `raw_input`, plus 7 test/spec hygiene patches. Final tally: 1743 unit pass + 1 brittle deselected + 1 pre-existing skip + 53 integration pass; 99.7% coverage on `nova.systems.nerve.system` (one residual SIGBREAK-uninstall partial branch); 100% on models.py + Rich skin. ruff + format + mypy strict all clean. Closes deferred-work.md:139.)
project: AI Assistant
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Project Foundation & Core Infrastructure
  epic-1: in-progress
  1-0-define-yaml-config-schemas-spike: done
  1-1-project-scaffolding-and-package-setup: done
  1-2-domain-exceptions-and-shared-types: done
  1-3-event-bus-and-typed-event-definitions: done
  1-4-sqlite-storage-engine: done
  1-5-migration-runner-and-initial-schema: done
  1-6-config-loader-and-immutable-novaconfig: done
  1-7-capability-tier-state-machine: done
  1-8-audit-logger: done
  1-9-port-interfaces-and-shield-stub: done
  1-10-composition-root-and-cli-entrypoint: done
  1-11-ci-quality-gate-automation: done
  epic-1-retrospective: done

  # Epic 2: First-Run Setup & Onboarding
  epic-2: in-progress
  2-1-setup-script-setup-bat: done
  2-2-api-key-configuration: done
  2-3-guided-mode-creation-wizard: done
  2-4-briefing-card-state-a-initial-capture-and-setup-completion: done
  2-5-api-key-update-post-setup: done
  epic-2-retrospective: done

  # Epic 3: Core Session Loop (Hero Path)
  epic-3: in-progress
  3-1-brain-session-and-seed-persistence: done
  3-2-briefingaggregate-and-state-determination: done
  3-3-briefingviewmodel-and-briefing-card-rendering: done
  3-4-t1-command-grammar-and-deterministic-parser: done
  3-5-nerve-command-routing-and-session-lifecycle: done
  3-6-mode-restore-and-app-launching: done
  3-7-shutdown-flow-and-seed-capture: backlog
  3-8-warm-resume-session-2-hero-moment: backlog
  3-9-status-command-and-help-display: backlog
  3-10-crash-recovery-and-unexpected-termination: backlog
  epic-3-retrospective: optional

  # Epic 4: Context Awareness & Memory Enrichment
  epic-4: backlog
  4-1-eyes-win32-context-capture: backlog
  4-2-exclusion-boundary-at-capture-layer: backlog
  4-3-workspace-snapshots-on-demand-and-context-buffer: backlog
  4-4-context-extraction-and-mode-inference: backlog
  4-5-memory-accumulation-and-enriched-briefings: backlog
  4-6-promptbuilder-trust-boundary: backlog
  4-7-bounded-memory-retention-and-pruning-policy: backlog
  epic-4-retrospective: optional

  # Epic 5: Transparency, Trust & Deletion
  epic-5: backlog
  5-1-transparency-command-and-knowledge-display: backlog
  5-2-selective-forget-with-atomic-deletion: backlog
  5-3-audit-trail-inspection: backlog
  5-4-tier-status-display-and-notification: backlog
  5-5-sqlite-corruption-recovery-flow: backlog
  5-6-backup-and-restore-user-facing-flow: backlog
  epic-5-retrospective: optional

  # Epic 6: Desktop Actions & Workspace Orchestration Expansion
  epic-6: backlog
  6-1-window-focus-and-arrange-in-mode-restore: backlog
  6-2-mode-state-bookmarking-on-switch: backlog
  6-3-ad-hoc-mode-creation: backlog
  6-4-mode-editing-via-command: backlog
  6-5-full-mode-configuration-folders-and-urls: backlog
  epic-6-retrospective: optional

  # Epic 7: Personality, Voice & Conversational Polish
  epic-7: backlog
  7-1-voice-system-and-personality-doctrine: backlog
  7-2-configurable-bluntness-levels: backlog
  7-3-strategic-praise-and-context-adaptive-style: backlog
  7-4-self-trimming-rituals: backlog
  epic-7-retrospective: optional

  # Epic 8: Capability Tiers & Graceful Degradation (Integration/Hardening)
  epic-8: backlog
  8-1-degraded-tier-integration-testing: backlog
  8-2-offline-tier-integration-testing: backlog
  8-3-tier-recovery-and-catch-up-briefing: backlog
  8-4-malformed-api-response-handling: backlog
  8-5-partial-restore-under-degraded-conditions: backlog
  8-6-user-and-developer-runbook: backlog
  epic-8-retrospective: optional
